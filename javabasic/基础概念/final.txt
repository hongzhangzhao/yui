# final 关键字的基本含义是: 这个数据/方法/类 不能被改变 了。

# final基本类型的数据: 定值 (constant value)，只能赋值一次，不能再被修改。

# final方法: 该方法不能被覆盖。private的方法默认为final的方法。

# final类: 该类不能被继承。

# 普通类型的对象也可以final,表示引用不能再被修改，只能指向一个对象，对象内容可以改变