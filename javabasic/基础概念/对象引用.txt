#  Person p = new Person();


# new 是在内存中(堆)为对象开辟一个空间，空间里面保存着对象的数据和方法；

# p 在(栈内存)不是对象本身，它类似一个指针，指向了堆内存中的对象空间；
       就是告诉 p 对象的地址。

# 这里的内存指的是JVM虚拟出来的Java进程内存空间。

# 基本类型的对象：保存在栈内存中。

  基本数据类型的 变量名 表示数据本事，不是引用。


# 接口也可以作为对象引用的类型，但是指向的对象必须实施了该接口